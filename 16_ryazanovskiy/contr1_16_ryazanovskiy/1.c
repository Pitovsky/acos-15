unsigned short int* convert(unsigned int* i)
{
    return i;
}


/*
#include <stdio.h>


int main()
{
    while (1)
    {
        unsigned int x = 0;
        scanf("%i", &x);
        unsigned short int p = *convert(&x);
        printf("%u\n", p);
    }
    return 0;
}
*/
