./main filename cols [rows] - открывает матрицу из filename размером cols x rows. Если файла не существует, создает его и заполняет нулями. Если rows не указан, вычисляет из размера файла.
Список и описание всех функций - по команде help.

